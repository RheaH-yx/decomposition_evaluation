// #include "./openql_decompose/include/ql/com/dec/unitary.h"
#include <iostream>
int main() {
    // std::vector<std::complex<double>> matrix = {1, 1, 1, -1}; // Hadamard gate matrix
    // Unitary::Unitary hadamard("Hadamard", matrix);

    // // Optionally, you can call `decompose` explicitly to decompose the gate:
    // hadamard.decompose();

    // return 0;
    std::cout << "main function\n";
}